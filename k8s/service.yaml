apiVersion: v1
kind: Service
metadata:
  name: devops3-service
spec:
  type: NodePort
  selector:
    app: devops3
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      nodePort: 30080
